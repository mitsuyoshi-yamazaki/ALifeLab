import { createMuiTheme } from "@material-ui/core"

export const defaultTheme = createMuiTheme({
  palette: {
    primary: {
      light: "#8a8a8a",
      main: "#575757",
      dark: "#454545",
    }
  }
})